{
	"name": "CACAO-JVM Dev Setup",
	"context": "..",
  // This sets the workspace folder to /code/cacao, so you don't have to 
  // open a new vscode window on startup at this location.
  "workspaceFolder": "/code/cacao"
}
